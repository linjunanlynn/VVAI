---
description: 项目 UI 术语全局定义（主CUI交互、主导航栏、主AI入口、组织状态）
alwaysApply: true
---

# CUI 项目全局术语

后续编码、评审与文档中须统一使用以下称谓（含书名号《》便于口头与文档对齐）：

## 《主导航栏》

- **所指**：界面**左侧**纵向主 Tab 区域（红绿灯、头像、消息、工作台、《主AI入口》、通讯录、我的，以及底部教育/待办/日历/更多等）。
- **实现参考**：`MainNavRail` 组件（`src/components/main-ai/MainNavRail.tsx`）。
- **说明**：不要简称混乱为「侧栏」「左边栏」等指代同一区域时，优先使用 **《主导航栏》**。

## 《主CUI交互》

- **所指**：《主导航栏》**右侧**的主内容区，即包含微微 AI 对话、VVAI 顶栏、输入区与底部应用条等**整块主界面交互区域**（相对《主导航栏》的剩余主体）。
- **实现参考**：`MainAIChatWindow` 内除《主导航栏》外的主列布局（如 `ChatNavBar`、会话区、`ChatSender`、底部应用条等）。
- **说明**：讨论「主界面」「聊天主区」且特指该主体时，使用 **《主CUI交互》**。

## 《主AI入口》

- **所指**：《主导航栏》中 **「工作台」Tab 下方**的圆形 AI 图标入口（渐变环 + AI 形象，无文字标签），用于回到主 AI / 主对话语境。
- **实现参考**：`MainNavRail` 内工作台与通讯录之间的 AI 按钮（`onAiHome` / `setActiveApp(null)` 等逻辑）。
- **说明**：不要与底部「教育」等二级入口或顶栏模型选择混淆；特指该图标时使用 **《主AI入口》**。

## 《组织状态》

- **所指**：**会话列表**头部展示的一块区域：是否已加入组织、当前主体、可切换的已加入组织列表，以及创建/加入组织的入口（顶栏不再重复承担主体切换时，由本模块承担）。
- **实现参考**：`SessionListOrgHeader`（`src/components/chat/SessionListOrgHeader.tsx`），由 `HistorySidebar` / `SecondaryAppHistorySidebar` 在侧栏主标题（如「会话列表」）**右侧固定宽度槽**内与标题**同行**展示（触发条为小号常规字重、弱色，相对标题弱化；可展开管理主体）。
- **说明**：讨论「会话列表里的组织」「侧栏组织切换」且特指该整块交互时，使用 **《组织状态》**；该称谓用于代码注释与文档对齐，**不在界面文案中展示**。

---

在代码注释中可写：`// 《主导航栏》`、`// 《主CUI交互》`、`// 《主AI入口》`、`// 《组织状态》`，与产品/设计文档保持一致。
